# Project Rules

## Automatic Git Commit & Push
After completing any code edit or task requested by the USER:
1. Stage modified files using `git add .`
2. Commit changes with a descriptive commit message: `git commit -m "<task summary>"`
3. Push changes to the remote repository: `git push`
